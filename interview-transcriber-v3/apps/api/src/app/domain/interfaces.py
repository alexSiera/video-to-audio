"""Domain interfaces (protocols) for repositories.

Repositories are defined here so that domain/application layers can reference them
without knowing the implementation (SQLAlchemy, in-memory, etc.).
"""
